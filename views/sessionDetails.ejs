<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sports Schedular</title>
  <link rel="stylesheet" href="./css/main/output.css">
  <script>
    function cancelSession(id) {
      
    }
  </script>
</head>
<body>
  <div class="grid grid-cols-12">
    <div class="col-start-2 col-span-10">
      <h2>Session #<%= details.id %></h2>
      <div>
        <p>Date & time :</p>
        <%= details.time %>
      </div>
      <div>
        <p>Address :</p>
        <%= details.place %>
      </div>
      <div>
        <h3>Players :</h3>
        <div class="flex">
          <% for( let i = 0; i < details.players.length; i++ ) { %>
            <%= details.players[i] %>
          <% } %>
        </div>
      </div>
      <div>
        <p>Empty slots : <%= details.noOfPlayers - details.players.length%></p>
      </div>
      <a href="#"><span>Edit Session</span></a>
      <a href="/sportSession" onclick="return cancelSession(<%= details.id %>)"><span>Cancel Session</span></a>
      
    </div>
  </div>
</body>
</html>